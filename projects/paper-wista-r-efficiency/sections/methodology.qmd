# Methodische Grundlagen und Bewertungsframework {#sec-methodology}

## Konzeptioneller Rahmen der Effizienzanalyse

Die Bewertung der Effizienz statistischer Verfahren erfordert einen systematischen Ansatz, der sowohl quantitative als auch qualitative Aspekte berücksichtigt. Im Kontext der amtlichen Statistik sind dabei verschiedene Dimensionen relevant [@eurostat2021quality]:

### Definition der Effizienzbegriffe

**Recheneffizienz** bezeichnet das Verhältnis zwischen dem erzielten statistischen Ergebnis und dem dafür erforderlichen Ressourcenaufwand (CPU-Zeit, Speicherverbrauch, I/O-Operationen).

**Statistische Effizienz** bezieht sich auf die Güte der statistischen Eigenschaften eines Verfahrens, insbesondere auf Bias, Varianz und Mean Square Error der Schätzer.

**Praktische Effizienz** umfasst Aspekte wie Implementierungsaufwand, Wartbarkeit und Robustheit gegenüber verschiedenen Datenstrukturen.

## Untersuchungsdesign

### Ausgewählte Verfahren

Für die empirische Analyse wurden folgende statistische Verfahren ausgewählt:

1. **Klassische Verfahren:**
   - Arithmetisches Mittel mit verschiedenen Gewichtungsschemen
   - Median und robuste Lageparameter
   - Standardabweichung und Varianzschätzer

2. **Optimierte Algorithmen:**
   - Numerisch stabile Implementierungen (Welford-Algorithmus)
   - Parallelisierte Berechnungsansätze
   - Approximative Verfahren für große Datensätze

3. **Moderne Ansätze:**
   - Online-Algorithmen für Datenströme
   - Sketching-Verfahren für Speicher-effiziente Berechnungen
   - Sampling-basierte Methoden

### Bewertungskriterien

Die Evaluierung erfolgt anhand eines mehrdimensionalen Kriterienkatalogs:

::: {.fullwidth}
```{r}
#| label: fig-evaluation-framework
#| fig-cap: "Mehrdimensionales Bewertungsframework für statistische Verfahren"
#| echo: false
#| warning: false
#| fig-width: 8
#| fig-height: 4

library(ggplot2)
library(dplyr)

# Erstelle Beispieldaten für das Bewertungsframework
criteria <- data.frame(
  Dimension = c("Rechenzeit", "Speicherverbrauch", "Statistische Güte", 
                "Skalierbarkeit", "Robustheit", "Implementierungsaufwand"),
  Gewichtung = c(0.25, 0.20, 0.30, 0.15, 0.05, 0.05),
  Kategorie = c("Performance", "Performance", "Qualität", 
                "Performance", "Qualität", "Praktikabilität")
)

ggplot(criteria, aes(x = reorder(Dimension, Gewichtung), y = Gewichtung, fill = Kategorie)) +
  geom_col() +
  coord_flip() +
  scale_fill_brewer(type = "qual", palette = "Set2") +
  labs(
    title = "Gewichtung der Bewertungskriterien",
    x = "Bewertungsdimension",
    y = "Relative Gewichtung",
    fill = "Kategorie"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(size = 12, hjust = 0.5),
    axis.text = element_text(size = 10),
    legend.position = "bottom"
  )
```
:::

### Experimentelle Durchführung

#### Testdaten

Die Experimente wurden mit synthetischen und realen Datensätzen durchgeführt:

- **Synthetische Daten:** Normalverteilte Zufallszahlen in verschiedenen Größenordnungen (10³ bis 10⁷ Observationen)
- **Reale Daten:** Anonymisierte Auszüge aus der Mikrozensus-Erhebung
- **Strukturierte Szenarien:** Verschiedene Verteilungsformen und Ausreißer-Konstellationen

#### Messmethodik

Alle Messungen wurden unter kontrollierten Bedingungen durchgeführt:

```r
# Beispiel für standardisierte Zeitmessung
benchmark_function <- function(func, data, n_iterations = 100) {
  times <- replicate(n_iterations, {
    start_time <- Sys.time()
    result <- func(data)
    end_time <- Sys.time()
    as.numeric(end_time - start_time, units = "secs")
  })
  
  list(
    mean_time = mean(times),
    median_time = median(times),
    sd_time = sd(times),
    result = result
  )
}
```

## Statistische Testverfahren

Zur statistischen Absicherung der Ergebnisse kommen verschiedene Testverfahren zum Einsatz:

### Vergleich der Rechenzeiten

Für den Vergleich der Ausführungszeiten zwischen verschiedenen Algorithmen wird der Wilcoxon-Vorzeichen-Rang-Test verwendet, da nicht von einer Normalverteilung der Messzeiten ausgegangen werden kann.

$$H_0: \text{median}(T_A - T_B) = 0$$

wobei $T_A$ und $T_B$ die Ausführungszeiten der Algorithmen A und B bezeichnen.

### Äquivalenztests für statistische Güte

Die statistische Äquivalenz der Ergebnisse wird mittels TOST (Two One-Sided Tests) geprüft:

$$H_0: |\theta_A - \theta_B| \geq \delta \text{ vs. } H_1: |\theta_A - \theta_B| < \delta$$

Der Äquivalenzbereich $\delta$ wird basierend auf fachlichen Überlegungen zur praktischen Relevanz festgelegt.

## Qualitätssicherung der Messungen

### Reproduzierbarkeit

Alle Experimente sind vollständig dokumentiert und unter Verwendung einer festen Seed-Einstellung für Zufallszahlengeneratoren reproduzierbar. Die verwendete Software-Umgebung wird mittels `renv` versioniert.

### Validierung

Die Korrektheit der Implementierungen wird durch Vergleich mit etablierten Referenzimplementierungen sichergestellt. Zusätzlich werden synthetische Testfälle mit bekannten analytischen Lösungen verwendet.

## Methodische Limitationen

Bei der Interpretation der Ergebnisse sind folgende methodische Einschränkungen zu beachten:

1. **Systemabhängigkeit:** Die Performance-Messungen sind spezifisch für die verwendete Hardware- und Software-Konfiguration.

2. **Datenstruktur:** Die Ergebnisse können je nach konkreter Datenstruktur und -verteilung variieren.

3. **Skalierungsverhalten:** Extrapolationen auf deutlich größere oder kleinere Datensätze sind nur bedingt möglich.

Das entwickelte Bewertungsframework bietet dennoch eine solide Grundlage für die systematische Evaluation statistischer Verfahren und kann als Entscheidungshilfe für die Auswahl geeigneter Algorithmen in der Praxis dienen.

## Beispiele für vollbreite Darstellungen

### Vollbreite Abbildung

::: {.fullwidth}
```{r}
#| label: fig-fullwidth-example
#| fig-cap: "Beispiel für eine vollbreite Abbildung: Komplexer Verfahrensvergleich"
#| echo: false
#| warning: false
#| fig-width: 12
#| fig-height: 6

library(ggplot2)
library(dplyr)

# Erstelle erweiterte Daten für eine komplexe Visualisierung
set.seed(123)
performance_extended <- expand.grid(
  Verfahren = c("Naive", "Optimiert", "Parallelisiert", "Online", "Sketching", "Hybrid"),
  Datensatz = c("Klein (10³)", "Mittel (10⁵)", "Groß (10⁷)"),
  Metrik = c("Ausführungszeit", "Speicherverbrauch")
) %>%
  mutate(
    Wert = case_when(
      Metrik == "Ausführungszeit" & Verfahren == "Naive" ~ c(0.001, 0.145, 24.567)[as.numeric(factor(Datensatz))],
      Metrik == "Ausführungszeit" & Verfahren == "Optimiert" ~ c(0.0008, 0.089, 15.234)[as.numeric(factor(Datensatz))],
      Metrik == "Ausführungszeit" & Verfahren == "Parallelisiert" ~ c(0.002, 0.034, 4.567)[as.numeric(factor(Datensatz))],
      Metrik == "Ausführungszeit" & Verfahren == "Online" ~ c(0.0005, 0.025, 2.123)[as.numeric(factor(Datensatz))],
      Metrik == "Ausführungszeit" & Verfahren == "Sketching" ~ c(0.0003, 0.008, 0.456)[as.numeric(factor(Datensatz))],
      Metrik == "Ausführungszeit" & Verfahren == "Hybrid" ~ c(0.0004, 0.015, 1.234)[as.numeric(factor(Datensatz))],
      
      Metrik == "Speicherverbrauch" & Verfahren == "Naive" ~ c(0.1, 12.5, 1250)[as.numeric(factor(Datensatz))],
      Metrik == "Speicherverbrauch" & Verfahren == "Optimiert" ~ c(0.08, 9.2, 920)[as.numeric(factor(Datensatz))],
      Metrik == "Speicherverbrauch" & Verfahren == "Parallelisiert" ~ c(0.2, 18.4, 1840)[as.numeric(factor(Datensatz))],
      Metrik == "Speicherverbrauch" & Verfahren == "Online" ~ c(0.001, 0.001, 0.001)[as.numeric(factor(Datensatz))],
      Metrik == "Speicherverbrauch" & Verfahren == "Sketching" ~ c(0.5, 0.5, 0.5)[as.numeric(factor(Datensatz))],
      Metrik == "Speicherverbrauch" & Verfahren == "Hybrid" ~ c(0.1, 2.1, 180)[as.numeric(factor(Datensatz))]
    )
  )

# Erstelle eine umfassende vollbreite Darstellung
ggplot(performance_extended, aes(x = Datensatz, y = Wert, fill = Verfahren)) +
  geom_col(position = "dodge") +
  facet_wrap(~Metrik, scales = "free_y", ncol = 2) +
  scale_y_log10() +
  scale_fill_brewer(type = "qual", palette = "Set2") +
  labs(
    title = "Vergleichende Leistungsanalyse verschiedener Algorithmen",
    subtitle = "Ausführungszeit und Speicherverbrauch nach Datensatzgröße",
    x = "Datensatzgröße",
    y = "Wert (logarithmische Skala)",
    fill = "Verfahren"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(size = 14, hjust = 0.5),
    plot.subtitle = element_text(size = 12, hjust = 0.5),
    axis.text.x = element_text(angle = 45, hjust = 1),
    legend.position = "bottom",
    strip.text = element_text(size = 12, face = "bold")
  )
```
:::

### Vollbreite Tabelle

::: {.fullwidth}
```{r}
#| label: tbl-fullwidth-example
#| tbl-cap: "Beispiel für eine vollbreite Tabelle: Detaillierte Leistungsmatrix aller Verfahren"
#| echo: false

# Erstelle erweiterte Tabellendaten
detailed_performance <- data.frame(
  Verfahren = c("Naive Implementierung", "Optimiert (Welford)", "Parallelisiert (OpenMP)", 
                "Online-Algorithmus", "Sketching-Verfahren", "Hybrid-Ansatz"),
  "Zeit_Klein" = c("0.001s", "0.0008s", "0.002s", "0.0005s", "0.0003s", "0.0004s"),
  "Zeit_Mittel" = c("0.145s", "0.089s", "0.034s", "0.025s", "0.008s", "0.015s"),
  "Zeit_Groß" = c("24.567s", "15.234s", "4.567s", "2.123s", "0.456s", "1.234s"),
  "Speicher_Klein" = c("0.1 MB", "0.08 MB", "0.2 MB", "0.001 MB", "0.5 MB", "0.1 MB"),
  "Speicher_Mittel" = c("12.5 MB", "9.2 MB", "18.4 MB", "0.001 MB", "0.5 MB", "2.1 MB"),
  "Speicher_Groß" = c("1250 MB", "920 MB", "1840 MB", "0.001 MB", "0.5 MB", "180 MB"),
  "Genauigkeit" = c("99.9%", "99.95%", "99.9%", "98.5%", "95.2%", "97.8%"),
  "Implementierungsaufwand" = c("Niedrig", "Mittel", "Hoch", "Mittel", "Hoch", "Sehr hoch"),
  "Robustheit" = c("Mittel", "Mittel", "Mittel", "Hoch", "Hoch", "Hoch"),
  "Skalierbarkeit" = c("Schlecht", "Schlecht", "Gut", "Sehr gut", "Sehr gut", "Sehr gut")
)

# Bessere Spaltennamen für die Ausgabe
colnames(detailed_performance) <- c(
  "Verfahren", 
  "Zeit (10³)", "Zeit (10⁵)", "Zeit (10⁷)",
  "RAM (10³)", "RAM (10⁵)", "RAM (10⁷)",
  "Genauigkeit", "Implement.", "Robustheit", "Skalierung"
)

knitr::kable(detailed_performance, 
      format = "latex",
      align = c("l", rep("r", 7), rep("c", 3)),
      booktabs = TRUE,
      longtable = TRUE) %>%
  kableExtra::kable_styling(latex_options = c("striped", "scale_down")) %>%
  kableExtra::add_header_above(c(" " = 1, "Ausführungszeit" = 3, "Speicherverbrauch" = 3, "Qualitätskriterien" = 3))
```
:::